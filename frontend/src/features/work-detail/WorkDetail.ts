// Work detail chunk entry. Load it only through `lazyWorkDetail` so every caller shares one dynamic import.
export { PersistedWorkDetailController } from "@/features/work-detail/PersistedWorkDetail";
export { RemoteOnlyWorkDetailController } from "@/features/work-detail/RemoteOnlyWorkDetail";
